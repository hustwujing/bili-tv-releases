# BiliTV Releases

This repository is the public, binary-only release channel for BiliTV Android TV.

Signed APKs are built and tested in the private source repository, then delivered
here through a repository-scoped deploy key. The public workflow verifies each
payload and publishes it as a GitHub Release for in-app updates.
